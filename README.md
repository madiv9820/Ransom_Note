# Ransom_Note
📝 Given two strings, ransomNote and magazine, return true ✅ if ransomNote can be constructed using the letters from magazine (each letter can be used only once 🔤), otherwise return false ❌.
